Simple landing page for the "Flying Chess" hack mod.

Files:
- index.html — main landing page (replace the download URL as needed)
- styles.css — basic styling

Preview: open `index.html` in a browser.

Replace the `https://example.com/download` link in `index.html` with your actual download reference link before publishing.
